# Exemplo de variáveis de ambiente — copie para .env.production e preencha
# NÃO commite o arquivo .env.production com valores reais

# URL base da API em produção (sem barra final)
VITE_API_BASE_URL=https://api.jamcore.app
